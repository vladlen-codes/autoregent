# Autoregent dashboard

Static Next.js export that polls the gateway's `/health` and `/events` client-side. See the [main README](../README.md#dashboard) for the full picture (local dev, GitHub Pages deploy, the backend CORS dependency).

```bash
npm install
npm run dev   # http://localhost:3000
```
